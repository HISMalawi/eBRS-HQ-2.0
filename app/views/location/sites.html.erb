<style>

.btn-row {
  width: 100%;
}

.panel-btns {
  height: 120px;
  width:  220px;
  font-size: 15px;
}

.panel-btns-main-containers {
  width: 100%;
}

.dashboard-title {
  margin-left: 1%;
}

.separator-line {
  width: 100%;
  border-style: solid;
  border-width: 1px 0px 0px 0px;
}

.panel-btns-containers {
  margin: 10px 55px 0px 0px;
}

</style>


<div class='dashboard-title'><h3>Locations</h3></div>

<div class='separator-line'>&nbsp;</div>


<div id='button-container' class="panel-btns-main-containers"></div>



    <%
      attributes = [
        ['Districts', 'dc'],
        ['Health facilities', 'fc'],
        ['Traditionl Authority', 'ta'],
        ['Villages', 'vl']
      ]
    %>


<script>
  var container = document.getElementById('button-container');

  var row_count = 1;
  var attributes = [];
  <%(attributes).each do |name, id|%>
    attributes.push(["<%=name%>", "<%=id%>"]);  
  <%end%>

  var row = document.createElement('div');
  row.setAttribute('class','row btn-row');
  container.appendChild(row);

  for(var i = 0; i < attributes.length; i++){
    var cell = document.createElement('div');
    cell.setAttribute('class','col-lg-2 panel-btns-containers');
    var btn = document.createElement('button');
    btn.setAttribute('type','button');
    btn.setAttribute('onclick',"document.location='/location/" + attributes[i][1] + "'");
    btn.setAttribute('class','btn btn-primary btn-lg panel-btns');
    btn.innerHTML = attributes[i][0];
    cell.appendChild(btn);
    row.appendChild(cell);

    if(row_count == 5) {
      /*var row = document.createElement('div');
      row.setAttribute('class','row btn-row');
      container.appendChild(row);
      row_count = 0;*/
    }
    row_count++;
  }

</script>
